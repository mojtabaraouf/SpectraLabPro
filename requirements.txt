# Core scientific stack
numpy>=1.20
matplotlib>=3.5
pandas>=1.4

# Spectrometer interface (Ocean Optics / Ocean Insight)
seabreeze>=2.0

# Recommended backend for stability and performance
seabreeze[cse]
Notes (do NOT include these in the file)
seabreeze[cse] enables the C backend and is strongly recommended
Works with Python 3.8+
Tkinter is included with standard Python distributions (no need to list)
If you want, I can also provide:
a minimal requirements.txt (no extras)
a conda environment.yml
or a frozen requirements-lock.txt for reproducibility
